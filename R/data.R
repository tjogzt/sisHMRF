#' Internal package data
#'
#' Curated immune gene sets used for feature construction. These are
#' automatically loaded via `get_immune_gene_sets()` and do not need to
#' be accessed directly.
#'
#' @name sisHMRF_data
#' @keywords internal
NULL


# Internal gene sets are built on-the-fly by get_immune_gene_sets().
# No .rda files are needed; the function definition in utils.R is the
# authoritative source.
